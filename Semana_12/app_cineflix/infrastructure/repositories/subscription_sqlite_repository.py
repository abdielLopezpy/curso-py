# ============================================================================
# IMPLEMENTACION: SubscriptionSQLiteRepository  -- TODO live coding
# ============================================================================
# Plantilla: ver infrastructure/repositories/movie_sqlite_repository.py
# Tabla: subscriptions (FK group_id, plan_id).
# Para "obtener_activa" puedes filtrar: WHERE group_id = ? AND is_active = 1
# ORDER BY expires_at DESC LIMIT 1.
# ============================================================================

# TODO
